// common use of regex match in js 

var contants = {
	
	IMG_MATCH : "<img[\s]+src[\s]*=[\s]*((['\"]?<src>[^'\"]*)[\"'])|(?<src>[^\s]*))", // seems wrong 
	
	URL_MATCH : "^(f|ht){1}(tp|tps):\/\/([\w-]+\.)+[\w]+(\/[\w-./?%&=]*)?",
	
};