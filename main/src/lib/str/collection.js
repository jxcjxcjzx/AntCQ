 // make it possible then 
 // make environment exported 
 var coreutil = JSrequire("find utils.js in main/core dir");


 function mergeArrayIntoCollection(array,collection){
 
	var arr[] = coreutil.CollectionUtils.toArray(array);
	for(Element elem : arr){
		collection.add(elem);
	}
 
 }